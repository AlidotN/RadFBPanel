@using System.Threading
@model RadFBApp.Models.ViewModels.Admin.LoginViewModel
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@{
    Layout = null;
    string lang_style = "";
    if (Thread.CurrentThread.CurrentCulture.ToString() == "en")
    {
        lang_style = "login-ltr";
    }
}


<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>@Texts.AdminPanel | @Texts.LoginPage</title>
    <!-- Tell the browser to be responsive to screen width -->
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <!-- Font Awesome -->
    <link rel="stylesheet" href="~/plugins/font-awesome/css/font-awesome.min.css">
    <!-- Theme style -->
    <link rel="stylesheet" href="~/dist/css/adminlte.min.css">
    <!-- iCheck -->
    <link rel="stylesheet" href="~/plugins/iCheck/square/blue.css">
    <!-- Google Font: Source Sans Pro -->
    <link href="https://fonts.googleapis.com/css?family=Source+Sans+Pro:300,400,400i,700" rel="stylesheet">

    <!-- bootstrap rtl -->
    <link rel="stylesheet" href="~/dist/css/bootstrap-rtl.min.css">
    <!-- template rtl version -->
    <link rel="stylesheet" href="~/dist/css/custom-style.css">
</head>

<body class="hold-transition login-page @lang_style">
    <div class="login-box">

        <div class="card">
            <div class="logo-wrapper">
                <img src="/dist/img/logo.png" alt="radfb" class="login-logo" />
            </div>
            <div class="card-body login-card-body">
                <div class="login-box-header">
                    <div class="language-wrapper">
                        @if (Thread.CurrentThread.CurrentCulture.ToString() == "en")
                        {
                            <a class="language-item active" asp-controller="Account" asp-action="ChangeLanguage" asp-route-lang="en">
                                <img src="/dist/img//Flag_USA_256.png" class="language-image" alt="USA" />
                                <span class="language-label">En</span>
                            </a>
                        }else
                        {
                            <a class="language-item" asp-controller="Account" asp-action="ChangeLanguage" asp-route-lang="en">
                                <img src="/dist/img//Flag_USA_256.png" class="language-image" alt="USA" />
                                <span class="language-label">En</span>
                            </a>
                        }
                        @if (Thread.CurrentThread.CurrentCulture.ToString() == "fa")
                        {
                            <a class="language-item active" asp-controller="Account" asp-action="ChangeLanguage" asp-route-lang="fa">
                                <img src="/dist//img/Flag_Iran_256.png" class="language-image" alt="IRAN" />
                                <span class="language-label">فا</span>
                            </a>
                        }else
                        {
                            <a class="language-item" asp-controller="Account" asp-action="ChangeLanguage" asp-route-lang="fa">
                                <img src="/dist//img/Flag_Iran_256.png" class="language-image" alt="IRAN" />
                                <span class="language-label">فا</span>
                            </a>
                        }

                    </div>
                    <p class="login-box-msg">@Texts.Fill_Login</p>
                </div>


                <form asp-action="Login" asp-controller="Account" asp-route-returnUrl="@ViewData["ReturnUrl"]" method="post" onsubmit="return validateForm()">

                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="input-group mb-3">
                        <div class="input-group-append">
                            <span class="fa fa-user input-group-text"></span>
                        </div>
                        <input asp-for="Email" id="InputEmail" class="form-control" placeholder="@Texts.User_Name" />

                        <span asp-validation-for="Email" class="text-danger"></span>
                    </div>

                    <div class="input-group mb-3">
                        <div class="input-group-append">
                            <span class="fa fa-lock input-group-text"></span>
                        </div>
                        <input asp-for="Password" id="InputPassword" class="form-control" placeholder="@Texts.Pass" />

                        <span asp-validation-for="Password" class="text-danger"></span>
                    </div>

                    <div class="row">
                        <div class="col-8">
                            <div class="checkbox icheck">
                                <label>

                                    <input asp-for="RememberMe" /> &nbsp; &nbsp;  @Texts.RememberMe

                                </label>
                            </div>
                        </div>

                        <!-- /.col -->
                        <div class="col-4">
                            <input type="submit" value="@Texts.Login" class="btn btn-primary btn-block btn-flat" />
                        </div>
                        <!-- /.col -->
                    </div>

                </form>


                <p class="mb-1">
                    <a href="#">@Texts.ForgottenMyPass</a>
                </p>

            </div>
            <!-- /.login-card-body -->
        </div>
    </div>
    <!-- /.login-box -->
    <!-- jQuery -->

    <script src="~/plugins/jquery/jquery.min.js"></script>
    <!-- Bootstrap 4 -->
    <script src="~/plugins/bootstrap/js/bootstrap.bundle.min.js"></script>
    <!-- iCheck -->
    <script src="~/plugins/iCheck/icheck.min.js"></script>
    <script>
        $(function () {
            $('input').iCheck({
                checkboxClass: 'icheckbox_square-blue',
                radioClass: 'iradio_square-blue',
                increaseArea: '20%' // optional
            })
        })
    </script>
</body>

</html>
